export type ApiMatch={id:number;utcDate:string;status:string;homeTeam:{id:number;name:string};awayTeam:{id:number;name:string};score?:{fullTime?:{home:number|null;away:number|null}};competition?:{name:string;code:string}};
type Standing={team:{id:number;name:string};position:number;playedGames:number;goalsFor:number;goalsAgainst:number;points:number};
async function footballFetch(path:string){const token=process.env.FOOTBALL_DATA_API_KEY;if(!token)return null;try{const res=await fetch("https://api.football-data.org/v4/"+path,{headers:{"X-Auth-Token":token},next:{revalidate:600}});if(!res.ok)return null;return res.json();}catch{return null;}}
export async function getMatches(dateFrom?:string,dateTo?:string){if(!process.env.FOOTBALL_DATA_API_KEY)return {matches:[] as ApiMatch[],source:"demo",message:"Add FOOTBALL_DATA_API_KEY in Vercel to enable live fixtures."};const p=new URLSearchParams();if(dateFrom)p.set("dateFrom",dateFrom);if(dateTo)p.set("dateTo",dateTo);const data=await footballFetch("matches?"+p.toString());if(!data)return {matches:[] as ApiMatch[],source:"football-data.org",message:"Live football data is temporarily unavailable. Try refresh."};return {matches:(data.matches??[]) as ApiMatch[],source:"football-data.org"};}
export async function getStandings(code:string){const data=await footballFetch("competitions/"+encodeURIComponent(code)+"/standings");return (data?.standings?.[0]?.table??[]) as Standing[];}
export async function getMatchesWithStats(){const result=await getMatches();if(!result.matches.length)return result;const codes=[...new Set(result.matches.map(m=>m.competition?.code).filter(Boolean))] as string[];const tables=new Map<string,Standing[]>();await Promise.all(codes.slice(0,10).map(async code=>tables.set(code,await getStandings(code))));const matches=result.matches.map(m=>{const table=tables.get(m.competition?.code||"")??[];const home=table.find(t=>t.team?.id===m.homeTeam.id);const away=table.find(t=>t.team?.id===m.awayTeam.id);return {...m,teamStats:home&&away?{home:{position:home.position,played:home.playedGames,points:home.points,goalsFor:home.goalsFor,goalsAgainst:home.goalsAgainst},away:{position:away.position,played:away.playedGames,points:away.points,goalsFor:away.goalsFor,goalsAgainst:away.goalsAgainst}}:null};});return {...result,matches};}
export async function getFootballAnalytics(){const now=new Date(),from=now.toISOString().slice(0,10),to=new Date(now.getTime()+14*86400000).toISOString().slice(0,10),result=await getMatches(from,to),matches=result.matches??[],finished=matches.filter(m=>m.status==="FINISHED"&&m.score?.fullTime?.home!=null&&m.score?.fullTime?.away!=null);let homeWins=0,draws=0,awayWins=0,goals=0;for(const m of finished){const h=m.score!.fullTime!.home!,a=m.score!.fullTime!.away!;goals+=h+a;if(h>a)homeWins++;else if(h===a)draws++;else awayWins++;}const n=finished.length;return {source:result.source,message:result.message,range:{from,to},analytics:{finished:n,upcoming:matches.filter(m=>m.status!=="FINISHED").length,teams:new Set(matches.flatMap(m=>[m.homeTeam.id,m.awayTeam.id])).size,goals,avgGoals:n?Number((goals/n).toFixed(2)):0,homeWinRate:n?homeWins/n:0,drawRate:n?draws/n:0,awayWinRate:n?awayWins/n:0}};}
